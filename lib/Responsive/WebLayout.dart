import 'package:flutter/material.dart';

class webView extends StatelessWidget {
  const webView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child:
       const Text('this is instagram for web')));
  }
}
